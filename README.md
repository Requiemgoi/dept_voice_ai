# DebtCall Automator - Система автоматизированного обзвона должников

Система для автоматизированного обзвона должников с использованием AI для распознавания речи и классификации ответов.

## 🏗️ Структура проекта

```
.
├── backend/              # FastAPI backend
│   ├── app/              # Основное приложение
│   │   ├── api/          # API endpoints
│   │   ├── core/         # Core бизнес-логика
│   │   ├── db/           # База данных
│   │   ├── models/       # SQLAlchemy модели
│   │   ├── schemas/      # Pydantic схемы
│   │   └── utils/        # Утилиты
│   ├── ml/               # ML модули (STT, классификатор)
│   ├── data/             # Данные (БД, аудио, файлы)
│   ├── scripts/          # Вспомогательные скрипты
│   ├── tests/            # Тесты
│   └── main.py           # Точка входа backend
├── frontend/             # React + TypeScript frontend
│   ├── src/
│   │   ├── api/          # API клиент
│   │   ├── components/   # React компоненты
│   │   ├── hooks/        # React хуки
│   │   ├── pages/        # Страницы приложения
│   │   ├── styles/       # CSS стили
│   │   ├── types/        # TypeScript типы
│   │   └── utils/        # Утилиты
│   └── public/           # Статические файлы
└── models/               # ML модели (не в Git - скачиваются отдельно)
```

---

## 🚀 Быстрый старт

### Требования

- **Python 3.10+** — для backend
- **Node.js 18+** — для frontend
- **Git** — для клонирования репозитория

---

### 1. Клонирование репозитория

```bash
git clone https://github.com/Requiemgoi/dept_voice_ai.git
cd dept_voice_ai
```

---

### 2. Backend

#### Windows:

```bash
# Создаем виртуальное окружение (из корня проекта)
python -m venv .venv

# Активируем его
.venv\Scripts\activate

# Устанавливаем зависимости
cd backend
pip install -r requirements.txt

# Копируем пример .env файла
copy .env.example .env

# Запуск (вариант 1 - через bat-скрипт)
start_clean.bat

# Запуск (вариант 2 - вручную)
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

#### Linux/Mac:

```bash
# Создаем виртуальное окружение (из корня проекта)
python3 -m venv .venv

# Активируем его
source .venv/bin/activate

# Устанавливаем зависимости
cd backend
pip install -r requirements.txt

# Копируем пример .env файла
cp .env.example .env

# Запуск
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

**Проверка работы Backend:**
- API документация: http://127.0.0.1:8000/docs
- Health check: http://127.0.0.1:8000/health

---

### 3. Frontend

```bash
# Переходим в папку frontend
cd frontend

# Устанавливаем зависимости
npm install

# Запуск (вариант 1 - через bat-скрипт, только Windows)
start_clean.bat

# Запуск (вариант 2 - вручную)
npm run dev
```

**Проверка работы Frontend:**
- Приложение: http://localhost:5173

---

### 4. ML Модели (опционально)

Модели Vosk для распознавания речи нужно скачать отдельно:

```bash
# Создаем папку models в корне проекта
mkdir models
cd models

# Скачиваем модели (русский и казахский)
# Русский: https://alphacephei.com/vosk/models/vosk-model-small-ru-0.22.zip
# Казахский: https://alphacephei.com/vosk/models/vosk-model-small-kz-0.15.zip

# Распаковываем архивы в папку models/
```

После распаковки структура должна быть:
```
models/
├── vosk-model-small-ru-0.22/
└── vosk-model-small-kz-0.15/
```

---

## 📋 API Endpoints

### Загрузка данных
- `POST /api/v1/upload` — Загрузка Excel файла с клиентами

### Работа с клиентами
- `GET /api/v1/clients` — Список клиентов (с пагинацией)
- `GET /api/v1/clients/{id}` — Детали клиента с историей звонков

### Обработка звонков
- `POST /api/v1/process/{id}` — Обработка одного клиента
- `POST /api/v1/process/{id}/response` — Загрузка аудио ответа
- `POST /api/v1/process/bulk` — Массовая обработка
- `GET /api/v1/process/bulk/{task_id}/status` — Статус массовой обработки

### Экспорт
- `GET /api/v1/export` — Экспорт результатов в Excel

### История и аналитика
- `GET /api/v1/history` — История звонков
- `GET /api/v1/analytics` — Статистика и аналитика

---

## 🔧 Конфигурация

### Переменные окружения (backend/.env)

```env
# Database
DATABASE_URL=sqlite+aiosqlite:///./data/db/app.db

# Paths (относительные от backend/)
AUDIO_STORAGE_PATH=./data/audio
UPLOAD_PATH=./data/uploads
EXPORT_PATH=./data/exports

# TTS Engine
TTS_ENGINE=espeak-ng
TTS_RATE=150

# API
API_HOST=0.0.0.0
API_PORT=8000

# CORS
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ML Models (относительные от корня проекта)
VOSK_MODEL_RU=models/vosk-model-small-ru-0.22
VOSK_MODEL_KK=models/vosk-model-small-kz-0.15
```

---

## 🛠️ Технологии

### Backend
- **FastAPI 0.109+** — Web framework
- **SQLAlchemy 2.0+** — ORM (async)
- **SQLite** — База данных (aiosqlite)
- **Pandas** — Работа с Excel
- **Loguru** — Логирование
- **espeak-ng** — Text-to-Speech

### Frontend
- **React 18** — UI библиотека
- **TypeScript** — Типизация
- **Vite** — Сборщик
- **TailwindCSS** — Стили
- **React Query** — Data fetching
- **Recharts** — Графики

### ML
- **Vosk** — Speech-to-Text
- **Классификатор** — Распознавание категорий ответов

---

## 📁 Формат Excel файла

Ожидаемые колонки:
- ФИО
- ИИН
- Кредитор
- Сумма
- Дни просрочки
- Телефон

---

## 🐛 Решение типичных проблем

### Порт уже занят
```bash
# Windows - найти процесс на порту 8000
netstat -ano | findstr :8000

# Убить процесс по PID
taskkill /F /PID <PID>
```

### Ошибки Python зависимостей
```bash
# Пересоздать виртуальное окружение
rmdir /s /q .venv   # Windows
rm -rf .venv        # Linux/Mac

python -m venv .venv
# Затем активировать и установить зависимости заново
```

### Ошибки npm
```bash
# Удалить node_modules и переустановить
rm -rf node_modules
npm install
```

---

## 👨‍💻 Команда

- **Backend:** НҰРЫМ
- **ML:** АРМАН Б
- **Frontend:** АРМАН А, МИРАС

---

## 📄 Лицензия

MIT License
